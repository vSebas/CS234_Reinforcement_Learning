\section{Suggested Figures}

\begin{itemize}
\item \textbf{Figure 1: System overview.} A diagram showing optimizer-generated data $\rightarrow$ DT training $\rightarrow$ autoregressive rollout $\rightarrow$ projection/fallback/validation $\rightarrow$ nonlinear solver.
\item \textbf{Figure 2: Offline loss curves across major runs.} Training and validation curves for the parent improved run, lateral fine-tune, and failure-conditioned fine-tune, highlighting that offline loss improved even when deployment did not.
\item \textbf{Figure 3: Baseline vs DT trajectory comparison near first failure.} A no-obstacle and an obstacle example where the DT takes the wrong lateral branch around $k\approx 10$--$15$.
\item \textbf{Figure 4: Acceptance diagnostics by start progress.} Scatter plot of start progress versus fallback count or projection total for the corrected relaxed-gate FT2 run, showing that acceptance clusters in narrow start regions.
\end{itemize}
